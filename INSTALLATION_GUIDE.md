# رليل تثبيت وبناء التطبيقات
# Installation & Build Guide

نذا الرليل الشامل يتضمن وسائل لبناء وتثبيت التطبيق على منصات مختلفة

## 🚀 بدء سريع

### 1. تثبيت المتطلبات

```bash
npm install
```

### 2. تشغيل التطبيق للتطوير

```bash
npm start
```
سيبدأ التطبيق على `http://localhost:8000`

---

## 🌐 PWA - تطبيق ويب متقدم

### التثبيت على الهواتف

#### الهاتف الذكية (الأندرويد والآيفون)

1. **فتح الخادمة**
   ```bash
   npm start
   ```

2. **التثبيت عبر متصفح**
   - افتح التطبيق في متصفح الهاتف (`Chrome` أو `Safari`)
   - انقر على رمز القائمة الثلاثية
   - انقر على "Add to Home Screen" أو "Install"
   - تم الانتهاء! سيظهر بالتطبيق على شاشة الرئيسية

#### بدع العمل بلا اتصال
- التطبيق ملقن بالكامل للعمل بلا إنترنت
- Service Worker يخزن البيانات تلقائياً
- سيتزامن مع الاتصال عند معاودة الاتصال

---

## 💻 برنامج سطح المكتب (Electron)

### التثبيت على مشعل رربون Windows/Mac/Linux

#### 1. البناء رللاختبار الليف
```bash
npm run electron-dev
```
سيفتح التطبيق في نافذة رسبه رللتطوير والادختبار

#### 2. بناء تطبيق للنشر

**Windows:**
```bash
npm run build -- --win nsis
```
سيتم إنشاء `parliament-app-2.0.0-setup.exe`

**macOS:**
```bash
npm run build -- --mac dmg
```
سيتم إنشاء `parliament-app-2.0.0.dmg`

**Linux:**
```bash
npm run build -- --linux AppImage
```
سيتم إنشاء `parliament-app-2.0.0.AppImage`

---

## 📱 تطبيقات الهاتف المحمول

### Android

#### المتطلبات:
- Java Development Kit (JDK 11+)
- Android Studio أو Android SDK
- Gradle

#### الخطوات:
```bash
npm install -g cordova
cordova create myapp com.parliament.requests "Parliament App"
cd myapp
cordova platform add android
cordova build android
```

سيتم إنشاء APK في: `platforms/android/app/build/outputs/apk/`

### iOS

#### المتطلبات:
- macOS
- Xcode
- CocoaPods

#### الخطوات:
```bash
npm install -g cordova
cordova create myapp com.parliament.requests "Parliament App"
cd myapp
cordova platform add ios
cordova build ios
```

سيتم إنشاء تطبيق iOS يمكن فتحه في Xcode

---

## 📦 الملفات المنتجة

| المنصة | الملف | الموقع |
|--------|-------|--------|
| PWA | متصفح فقط | `http://localhost:8000` |
| Windows | `.exe` | `dist/parliament-app-2.0.0-setup.exe` |
| macOS | `.dmg` | `dist/parliament-app-2.0.0.dmg` |
| Linux | `.AppImage` | `dist/parliament-app-2.0.0.AppImage` |
| Android | `.apk` | `platforms/android/app/build/outputs/apk/` |
| iOS | `.ipa` | `platforms/ios/build/` |

---

## 🔧 أوامر البناء المتقدمة

### تنظيف الملفات القديمة
```bash
rm -rf node_modules dist
npm install
```

### بناء الكل
```bash
npm run build
```

### اختبار الأداء
```bash
npm run lint
npm run test
```

---

## ⚙️ نصائح التثبيت

1. **قبل البناء**: تأكد من نسخ الملفات بشكل صحيح
2. **متطلبات النظام**: تأكد من تثبيت Node.js v16+
3. **الإصدارات**: تحديث جميع المكتبات بانتظام
4. **الأخطاء**: تحقق من رسائل الخطأ في `node_modules/` و `dist/`

---

## 📝 ملاحظات

- التطبيق يدعم العربية كاملة
- جميع التطبيقات تتصل بـ Firebase بشكل آمن
- الوضع غير المتصل مدعوم تماماً
- واجهة المستخدم تتكيف مع جميع أحجام الشاشات

---

**آخر تحديث**: يناير 2026
**الإصدار**: 2.0.0
