name: Run Telegram Bot

on:
  schedule:
    - cron: '*/50 * * * *'   # يشتغل كل 50 دقيقة تلقائياً
  workflow_dispatch:           # تشغيل يدوي من GitHub لو أردت

jobs:
  run-bot:
    runs-on: ubuntu-latest
    timeout-minutes: 50        # يشتغل 50 دقيقة ثم يعيد التشغيل

    # تم إضافة هذا القسم لحل مشكلة إصدار Node.js
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          FIREBASE_CREDENTIALS_JSON: ${{ secrets.FIREBASE_CREDENTIALS_JSON }}
          FIREBASE_URL: ${{ secrets.FIREBASE_URL }}
          BOT_PASSWORD: ${{ secrets.BOT_PASSWORD }}
          ARCHIVE_CHANNEL_ID: ${{ secrets.ARCHIVE_CHANNEL_ID }}
        run: python bot.py
